import React, { Component } from "react";
import { Grid } from "semantic-ui-react";

class Channel extends Component {
  render() {
    return (
      <Grid textAlign="center">
        <Grid.Column>
          <Grid.Row>sample</Grid.Row>
        </Grid.Column>
      </Grid>
    );
  }
}

export default Channel;
