const INITIAL_STATE = {};

const reducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    case "action.type":
      break;
    default:
      return state;
  }
};

export default reducer;
